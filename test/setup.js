/**
 * Test setup test
 * TODO: Please remove the file after first test where included in the project.
 */

import assert from 'assert';

describe('The test setup', function() {
  it('should successfully run this test', function() {
    assert.equal(1, 1);
  });
});
