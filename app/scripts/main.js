import Map from './map';

class App {
  /**
   * Constructs the app.
   */
  constructor() {
    this.map = new Map('.map');
  }
}

/* eslint-disable no-unused-vars */
let app = new App();
/* eslint-enable no-unused-vars */
